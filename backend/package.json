{
  "name": "student-card-management-api",
  "version": "1.0.0",
  "description": "Backend API for Student Card Management System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup-db": "node scripts/setup-db.js",
    "migrate": "sudo -u postgres psql -d student_card_management -f migrations/schema.sql",
    "seed": "sudo -u postgres psql -d student_card_management -f migrations/seed.sql"
  },
  "keywords": ["student", "management", "api"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

